<UserControl x:Class="ExamSheduleDesign.Controls.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             Width="300" Height="Auto">
    <UserControl.Resources>
        <Storyboard x:Key="ShowAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="320" To="0" Duration="0:0:0.25"
                             DecelerationRatio="0.8"/>
        </Storyboard>
        <Storyboard x:Key="HideAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="0" To="320" Duration="0:0:0.25"
                             DecelerationRatio="0.8">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Border Background="{StaticResource CardBackgroundBrush}"
            CornerRadius="8"
            BorderBrush="{StaticResource AccentBrush}"
            BorderThickness="0,0,0,3"
            Effect="{StaticResource CardShadow}"
            Padding="12,8">
        <StackPanel Orientation="Horizontal">
            <iconPacks:PackIconModern Kind="Information" Width="20" Height="20" Foreground="{StaticResource AccentBrush}" Margin="0,0,12,0"/>
            <TextBlock x:Name="MessageText" Foreground="{StaticResource PrimaryTextBrush}" FontSize="13" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</UserControl>