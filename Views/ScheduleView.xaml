<UserControl x:Class="ExamSheduleDesign.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:ExamSheduleDesign.Converters"
             xmlns:viewmodels="clr-namespace:ExamSheduleDesign.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:ScheduleViewModel}">

    <UserControl.Resources>
        <converters:BooleanConverter x:Key="BooleanConverter"/>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель сортировки -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="12,8" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Сортировать по:" VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding SortColumns}" SelectedItem="{Binding SortColumn}" Width="180" Margin="0,0,12,0"/>
                <Button Content="По возрастанию" Command="{Binding SortAscendingCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="По убыванию" Command="{Binding SortDescendingCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" Style="{StaticResource DataGridStyle}" ItemsSource="{Binding Exams}" AutoGenerateColumns="False" CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header=" " Width="40" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>

                <DataGridTextColumn Header="Преподаватель 1" Width="150" Binding="{Binding Teacher1.FullName}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTextColumn Header="Преподаватель 2" Width="150" Binding="{Binding Teacher2.FullName}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTextColumn Header="Дата" Width="100" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTextColumn Header="Дисциплина" Width="150" Binding="{Binding Discipline.Name}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTextColumn Header="Группа" Width="100" Binding="{Binding Group.Name}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTextColumn Header="Отделение" Width="100" Binding="{Binding Group.Department}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTextColumn Header="Время" Width="80" Binding="{Binding Time}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTextColumn Header="Аудитория" Width="80" Binding="{Binding Classroom}" CellStyle="{StaticResource DataGridCellStyle}"/>
                <DataGridTemplateColumn Header="Тип" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="12" Padding="8,2" Background="{StaticResource AccentBrush}" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Type}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="12,12" Margin="0,16,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="Выбрано:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding SelectedCount}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="Удалить экзамен" Command="{Binding DeleteSelectedCommand}" Style="{StaticResource DangerButtonStyle}" 
                            IsEnabled="{Binding SelectedCount, Converter={StaticResource BooleanConverter}}"/>
                </StackPanel>
                <Button Content="Создать файл расписания" Command="{Binding CreateScheduleFileCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>