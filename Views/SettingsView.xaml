<UserControl x:Class="ExamSheduleDesign.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:ExamSheduleDesign.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}">

    <Border Style="{StaticResource CardStyle}" Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,16" TextAlignment="Center">
                Настройки подключения
            </TextBlock>
            <Button Content="Переподключиться к серверу" 
                    Command="{Binding ReconnectCommand}" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Height="40"/>
        </StackPanel>
    </Border>
</UserControl>